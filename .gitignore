# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Frontend
node_modules/
frontend/dist/

# Test output
tests/screenshots/
tests/node_modules/
tests/package*.json

# Documentation build (the PDFs themselves are committed as deliverables)
docs/node_modules/
docs/package*.json
docs/pdf/*.html

# IWMI campaign field data. Kept out of the repository deliberately: the app
# reads it from this directory when it is present (backend/app/campaign.py) and
# offers a generated sample when it is not, so nothing here needs committing.
Data/

# Office lock files and OS cruft
~$*
Thumbs.db
.DS_Store
